import React from "react";

export default function Filter() {
  return (
    <div className="field">
      <button onClick={this.props.renderingGreasePig} >
        Show Greased Pigs
      </button>
    </div>
  );
}
